# plotting_satellite_optical

Notebooks for plotting and comparing optical satellite scenes (Sentinel-2, Landsat 8, MODIS).

- `plot_rgb.ipynb` — RGB composites from raw bands.
- `compare_satellite_scenes.ipynb` — side-by-side scene comparison.

Imagery folders are gitignored. See `environment.yml` for dependencies.
