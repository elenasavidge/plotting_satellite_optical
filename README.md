# plotting_satellite_optical

Notebook for plotting and comparing optical satellite scenes (Sentinel-2, Landsat 8, MODIS).

- `compare_satellite_scenes.ipynb` — side-by-side scene comparison with RGB composites from raw bands.

Imagery folders are gitignored. See `environment.yml` for dependencies.
